\documentclass{article}
\begin{document}
This is a deliberately very very very very very very very very very very long line that exceeds one hundred characters.
Thus $x=3.$ and $a,b,$ are wrong.
This has "literal quotes" in it.
We have $sin(x) + \cos(y) = 0$ here.
Use $x \ldots y$ and $a \cdots b$.
Then $a || b$ in math.
$$x = 1$$
\[ x = 1 \]
\[ y = 2 \]
Now we compute \begin{align*}
  a &= b \\
  c &= d
\end{align*} which is wrong.
\begin{itemize}
\ii bad indent here
\end{itemize}
Set $f : \mathbb{R} \to \mathbb{R}$.
We have $x =3$ asymmetric.
Space before , punct.
First paragraph.\\\\
Second paragraph.

\begin{asy}
dot("$A$", A, dir(A));
\end{asy}
End.   
\end{document}
